Enhancements:
  - Debloat.
  - Currently communicates in both HTTP requests and MQTT, standardise to just one.

Desired Features:
  - Add error handling and reporting (maybe with leds?).
  - Make some values customisable through an admin console, such as all the urls and the door unlocking delay.
  - OTA updates.
  - Manage cards and access on the admin console, then simplify the ESP's work
